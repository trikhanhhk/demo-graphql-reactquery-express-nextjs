import { ClassItem } from "@/type";
import React from "react";

interface Props {
  classItem: ClassItem;
}

function DetailClass({ classItem }: Props) {
  return <div>
    
  </div>;
}

export default DetailClass;
